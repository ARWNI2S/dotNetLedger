namespace dotNetLedger.Ethereum
{
    public sealed class EVMLedgerAdapter : LedgerAdapter, ILedgerAdapter
    {
        //private readonly RpcClient _rpcClient;

        //public EVMLedgerAdapter(RpcClient rpcClient)
        //{
        //    _rpcClient = new RpcClient(baseUri, authHeaderValue, ;
        //}
    }
}
